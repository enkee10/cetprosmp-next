# Settings for Backend (on Cloud Run).
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables.
env:
  - variable: MAINTENANCE_MODE
    value: "true"
    availability:
      - BUILD
      - RUNTIME