// src/app/(aplicacion)/carreras/layout.tsx
import { metadata } from './metadata';

export { metadata };

export default function CarreraesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
