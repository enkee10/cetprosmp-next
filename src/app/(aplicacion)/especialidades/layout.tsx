// src/app/(aplicacion)/especialidades/layout.tsx
import { metadata } from './metadata';

export { metadata };

export default function EspecialidadesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
